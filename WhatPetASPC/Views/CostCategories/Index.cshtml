@model IEnumerable<WhatPetASPC.Models.CostCategories>
@{
    ViewBag.Title = "Index";
}
<h2>Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CostBracket)
        </th>
        <th></th>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CostBracket)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CostID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CostID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CostID })
        </td>
    </tr>
}
</table>